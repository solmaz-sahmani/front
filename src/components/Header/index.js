import "./index.css";
import React from 'react';
import Button from "../Btn";

const Header = () => {
  return (
      <div className="header">
      <Button/>
      </div>
  );
};

export default Header;
